border-line($f, $color)
  background-image linear-gradient(180deg, $color, $color 50%, transparent 50%)
  background-size 100% 1px
  background-repeat no-repeat
  background-position $f left
  background-origin border-box
